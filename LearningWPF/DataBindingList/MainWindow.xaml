<Window x:Class="DataBindingList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataBindingList"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <ComboBox Name="Students" ItemsSource="{Binding}" FontSize="20" Height="50" Width="200">
            <ComboBox.ItemTemplate>
            <DataTemplate>
            <StackPanel Orientation="Horizontal" Margin="2">
                <TextBlock Text="Name" Margin="2"/>
                <TextBlock Text="{Binding Name}"  Margin="2"/>
                <TextBlock Text="Age" Margin="2" />
                <TextBlock Text="{Binding Age}"  Margin="2"  />
            </StackPanel>
            </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        
    </Grid>
</Window>
